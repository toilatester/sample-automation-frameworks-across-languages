/**
 * This file is a short reference for import all page object and component
 */

const { LoginPage } = require('./sampleapp.login/login.page');
const { HomePage } = require('./sampleapp.home/home.page');

module.exports = {
  LoginPage,
  HomePage,
};
